{
  "name": "snue-cafeteria-crawler",
  "version": "1.0.0",
  "description": "Scraper for SNUE cafeteria meal data → Firestore",
  "scripts": {
    "start": "node scraper.js",
    "dry-run": "node scraper.js --dry-run"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "playwright": "^1.53.0"
  },
  "engines": {
    "node": ">=20"
  }
}
